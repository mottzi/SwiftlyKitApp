# SwiftlyKit UI mockup prompts

Generated with the built-in imagegen tool. The reference is a computer-use screenshot of the existing app at 500 points wide. These are design concepts, not implementation screenshots or pixel measurements.

## 01-build-result-and-export

Use case: ui-mockup. Produce one high-resolution landscape before/after design board, approximately 1800x1100. The latest attached image is a REFERENCE for the existing SwiftlyKit macOS app, not an image to decorate. Recreate its native dark macOS utility appearance closely: system San Francisco typography, charcoal window, orange Swift symbol, native traffic lights, modest thin panel outlines, tight spacing and small controls. Crisp legible UI text, flat front view, no perspective or device frame.
Board title outside the app: "Build result and export". Two equally sized app windows side by side labeled "Current" and "Proposed". Both depict a logical 500 x 485 point window, with identical positions and sizes. Both must keep the exact reference two-part layout: top package card y36 to212; bottom build card y222 to473. Status row 48 points tall at top of bottom card, console toolbar 27 points tall, console below. Do not enlarge either window, add panels, a sidebar, tabs, permanent rows, or a new toolbar.
Both windows show illustrative package "ServerPackage", path "~/Projects/ServerPackage", Product "Server", Target "ARM64 Linux", Configuration "Release", Swift "Automatic", Strip Binary on. Preserve reference two-column form placement, five small information accessories and existing circular play button in package header. These choices apply to a NEXT build. The last completed build was for x86_64 Linux.
CURRENT bottom status: green check, title "Build succeeded", subtitle "~/Projects/ServerPackage/.build/.../.Server.swiftlykit-stripped", small circular Finder folder action in the existing far-right slot. Console has restrained monospaced build output, final green "Build succeeded.".
PROPOSED bottom status: same green check and same exact row bounds, title "Last build succeeded", subtitle "Server · x86_64 Linux · Release". The same single 24-point circular folder action now opens a compact native dark popover; show it open, attached to that slot, extending down over existing console content WITHOUT moving or resizing anything. Popover around 280 x 180 logical points: title "Build files", two small file rows "Server" and "ServerPackage_Server.resources", caption "Keep these files together.", divider, primary blue native "Export Build…" button and secondary "Show in Finder" action. Do not depict the popover as a permanent third panel. Final filename is "Server", with no dot prefix or internal suffix.
Outside the windows, a short caption: "Export the executable and its required resources together." Tiny label "Concept mockup · illustrative package". Do not add annotation badges or callouts inside app. Match reference density, alignment and physical proportions. Not a wholesale visual redesign.

## 02-setup-and-error-feedback

Use case: ui-mockup. Generate one crisp 2048x2048 before/after comparison board of the dark native SwiftlyKit macOS utility. Latest attached screenshot is the visual REFERENCE. Closely preserve SF typography, charcoal surfaces, thin rounded panel borders, orange Swift mark, circular play button, native controls, muted labels, and the two stacked cards. No glossy redesign, sidebar, tabs, new toolbar or banners.
Board title outside all windows: "Status that explains the next step". Grid of FOUR equally sized complete app windows, two columns and two rows. Column headings "Current" and "Proposed". Row headings outside windows "Package has no executable" and "Compiler error". Each window represents the same 500 x 485 logical-point bounds as reference. TOP card y36..212 and BOTTOM card y222..473 identical in all four. Bottom status strip MUST stay48 points high with exactly two lines and ONE24-point action slot; console toolbar27points. All text must be clean and readable.
First row BOTH windows package "ServerLibrary", path "~/Projects/ServerLibrary", Product "—" disabled, Target "x86_64 Linux", Configuration "Release", Swift "Automatic", Strip Binary off. Preserve two-column field layout and five accessory positions. Console empty with "Build output appears here.".
First row CURRENT status: subdued hammer, title "Build", subtitle "Complete package discovery to enable building.", no action. Product accessory ordinary tiny info circle.
First row PROPOSED status: amber info icon, title "No executable products", subtitle "Choose another package.", right action only a small circular folder-open icon that opens the chooser. Keep help accessory and upper UI locations untouched. No big Choose button and no added line.
Second row BOTH windows package "ServerPackage", path "~/Projects/ServerPackage", Product "Server", Target "x86_64 Linux", Configuration "Release", Swift "Automatic", Strip Binary off.
Second row CURRENT status: small red warning, title "Build failed", subtitle "SwiftPM could not build the executable: Building for production…". Console visibly includes normal progress and diagnostic once, then duplicated as a red multi-line block: "Building for production…" / "Compiling Server main.swift" / "main.swift:1:7: error: cannot find 'missingAuditSymbol' in scope". Red duplication should be visibly dense but no giant fonts.
Second row PROPOSED status same row geometry: red warning, title "Build failed", subtitle "Cannot find 'missingAuditSymbol' in scope.", small circular information/detail button at existing right slot. Console shows muted command "$ swift build --product Server", ordinary progress "Compiling Server main.swift", source location "main.swift:1:7", the error once in restrained red "error: cannot find 'missingAuditSymbol' in scope", then code "print(missingAuditSymbol)" and a short "Build failed." line. No duplicate transcript and no extra inline error panel. Keep remaining console space empty.
One external bottom caption: "The existing 48-point status row carries the explanation." Tiny "Concept mockups". Make comparisons faithful and restrained.

## 03-package-choice-and-preservation

Use case: ui-mockup. Generate a high-resolution landscape comparison board about SwiftlyKit's package workflow, approximately1800x1100. Latest attached screenshot is the existing UI REFERENCE. Copy its native dark macOS utility styling and exact stacked two-card layout, typography and compact proportions. Keep every top form row, panel boundary and existing toolbar position. No sidebar, progress wizard, tabs, permanent notifications, extra buttons or larger window.
Outside windows title "Keep the package workflow short". Two identical logical500x485 windows side by side, labeled "Current" and "Proposed". Both show package "MultipleExecutables", path "~/Projects/MultipleExecutables", Configuration "Release", Swift "Automatic", Strip Binary off, and Target "ARM64 Linux". All top control positions match reference. Circular primary play/build action remains in place.
This board illustrates changing the target AFTER the developer had selected product Beta. CURRENT Product value is "Alpha". Status "Ready to build", subtitle "Alpha · ARM64 Linux · Release · Swift 6.3.3". PROPOSED Product value remains "Beta". Status "Ready to build", subtitle "Beta · ARM64 Linux · Release · Swift 6.3.3". The before/after must visibly show Alpha versus Beta. Do NOT add a 'selection preserved' badge inside the UI.
On both windows show the native package action menu open from the tiny ellipsis next to package name. Menus overlay content without moving underlying form. CURRENT menu exactly: "Show in Finder", "Copy Path", separator, "Clean Build Artifacts…", "Reset Build Storage…", separator, "Close Package". PROPOSED menu exactly: "Choose Another Package…    ⌘O", separator, "Show in Finder", "Copy Path", separator, "Clean Build Artifacts…", "Reset Build Storage…", separator, "Close Package". Leave enough of the product field visible to see the comparison. Align menus to the header ellipsis, narrow native menu proportions, not giant panels.
Beneath CURRENT window outside app: "Changing packages: Close → Select → Choose". Beneath PROPOSED window outside app: "Changing packages: Choose another → Choose". Short shared caption outside: "Changing architecture keeps Beta selected when it is still available." Small "Concept mockup". A practical UI proposal, not marketing art. Crisp text and faithful existing geometry.
