import SwiftUI

struct BuildSection: View {

    var body: some View {
        RoundedRectangle(cornerRadius: 12)
            .foregroundStyle(.secondary)
            .padding(.horizontal)
    }
    
}
